import { AppContainer } from 'codefee-kit';
import Routes from './Routes';
import 'index.scss';

const App = () => {
  return (
    <AppContainer>
      <Routes />
    </AppContainer>
  );
};

export default App;
